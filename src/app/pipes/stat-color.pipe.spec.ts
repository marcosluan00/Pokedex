import { StatColorPipe } from './stat-color.pipe';

describe('StatColorPipe', () => {
  it('create an instance', () => {
    const pipe = new StatColorPipe();
    expect(pipe).toBeTruthy();
  });
});
